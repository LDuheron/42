#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

va_list	pipex
{
	int	pid;
	infile
	outfile

}

int pipe()
{
	// printf("parent --> %d\n", getpid());

	pipex->pid = fork();
    if (pid == -1) {
    {
		perror("fork");
        exit(EXIT_FAILURE);
	}
	if (pipex->pid1 < 0)
		return (1); // creer conditon derreur 
	else if (pipex->pid == 0)
	{
		printf("Je suis un vieux monsieur et mon pid est %d et mon enfant est %d\n", getpid(), pid1);
	}
	{
		printf("Je suis un petit enfant et mon pid est %d\n", getpid());
	}
	else if
	{
		printf("Je suis un vieux monsieur et mon pid est %d et mon enfant est %d\n", getpid(), pid1);
	}


}

int	main()
{
	
	if (argc (!= 5))
	{
		strerror(0):
		return (NULL);
	}
	creer un pipe avec la foncton ppre taille 2 pour le stdin, 1 sortie dntree et 2 = sortie derreur)
	memset pour faire pointer vers null tout les pointeurs
	foncton pour intialiser tous les parametres dont jai besoin
	execv(path, ma commande et mon environenemnt)
	-> creer une fonctionqui va aller dans lenvironnement et regarder ligne par ligne si cest le bon ou pas , a chaque fois separe en :, avec access cherche rsi bien dans lexecutable, utiliser fok et accessok, join commande, access pour voir si ca marche et s = condition verifier qu otut return 1 car 0 = echec; rejouter un / quand bonne commande pour terminer le path:
	en deuxieme parametre donner ma commande en argv2 et ensuite donner lenvironnement.

	
	pipe() == 1
	waitpid === free
}


fonction pipfok, s, 

free si ereur 